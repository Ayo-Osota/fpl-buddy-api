services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fpl_buddy
      POSTGRES_PASSWORD: fpl_buddy
      POSTGRES_DB: fpl_buddy
    ports:
      - "5432:5432"
    volumes:
      - fpl_buddy_pg_data:/var/lib/postgresql/data

volumes:
  fpl_buddy_pg_data:
